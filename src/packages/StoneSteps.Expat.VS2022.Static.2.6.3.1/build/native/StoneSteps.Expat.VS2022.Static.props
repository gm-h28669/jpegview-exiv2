<?xml version="1.0" encoding="utf-8"?>
<Project DefaultTargets="Build" ToolsVersion="14.0" xmlns="http://schemas.microsoft.com/developer/msbuild/2003">
  <PropertyGroup Label="UserMacros">
    <!-- libexpat for all configurations (Debug version is renamed from libexpatdMD) -->
    <LibExpatName>libexpatMD</LibExpatName>
    <LibExpatDir>$(MSBuildThisFileDirectory)</LibExpatDir>
  </PropertyGroup>
</Project>